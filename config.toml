baseURL = "https://www.tartandictionary.org/"
locale = "en"
DefaultContentLanguage = "en"
theme = "TartanDictionary"
title = "Tartan Dictionary"
enableInlineShortcodes = true
enableEmoji = true
googleAnalytics = ""

# Keep table headers below the top-level scalars: anything after a [table] line belongs to that
# table, which once silently swallowed title & co into [pagination].
[pagination]
pagerSize = 20

[params]
  lazyImage = true
  enableMathJax = false # enable it in Front Matter
  enableReadingTime = true
  favicon = "/favicon.png" # the six-colour Dictionary sett, woven by tartan-weaver's `task site:assets`
  startyear = "2022"
  description = "A simple and concise hugo theme."
  disqus = ""

[params.valine]
  enable = false
  lang = 'en' 
  appId = ''
  appKey = ''
  placeholder = 'Say Something......'
  requiredFields = ['nick', 'mail']
  avatar = 'robohash'
  visitor = true
  serverURLS = 'https://abc123.api.lncldglobal.com'
  debug = false # display the visitor counts when using 'hugo server'

[menu]
  # No "Home" entry — the Tartan Dictionary wordmark/logo is the home link.
  # "Clan" groups the named-after-something tartans. The label is loose Scottish
  # flavour — the children are Family / Clans / Corporate / Other, not only clans.
  [[menu.main]]
  name = "Clan"
  identifier = "clan"
  url = "#"
  weight = 1
    [[menu.main]]
    parent = "clan"
    name = "Family"
    url = "/families/"
    weight = 1
    [[menu.main]]
    parent = "clan"
    name = "Clans"
    url = "/clan/"
    weight = 2
    [[menu.main]]
    parent = "clan"
    name = "Corporate"
    url = "/type/corporate/"
    weight = 3
    [[menu.main]]
    parent = "clan"
    name = "Other"
    url = "/type/other/"
    weight = 4

  # "Tartans" groups the structure-based views of the cloth itself.
  # Setts stays out of the menu (the name search and TTD navigator usurped its listing);
  # /setts/ remains as every variant's address space.
  [[menu.main]]
  name = "Tartans"
  identifier = "tartans"
  url = "#"
  weight = 2
    [[menu.main]]
    parent = "tartans"
    name = "Stripes"
    url = "/stripes/"
    weight = 1
    # Overstripes — "coming soon" holding page (layout: holding) until the real
    # content is built. See content/overstripes/.
    [[menu.main]]
    parent = "tartans"
    name = "Overstripes"
    url = "/overstripes/"
    weight = 2
    # Setts — the /setts/<bare-sett>/ tier: proportions recorded at more than one scale,
    # each gathering its setts. Generated by internal/dictionary writeTartans.
    [[menu.main]]
    parent = "tartans"
    name = "Setts"
    url = "/setts/"
    weight = 3
    # Named tartans — the /tartans/<name>/ tier (#69b): one page per named design that groups
    # more than one sett (its kilt, Carpet, Child's…). Generated by GenerateTartans.
    [[menu.main]]
    parent = "tartans"
    name = "Named tartans"
    url = "/tartans/"
    weight = 4
    # Match a tartan to a logo or brand colours — the in-browser matcher at /logo/.
    [[menu.main]]
    parent = "tartans"
    name = "Match a logo"
    url = "/logo/"
    weight = 5

  # TTD (the tartan navigator) is its own top-level entry.
  [[menu.main]]
  name = "TTD"
  url = "/ttd/"
  weight = 3

  # Editorial blog (was "Posts"), sits to the right of TTD.
  [[menu.main]]
  name = "Blog"
  url = "/posts/"
  weight = 4

# Right-hand "Info" menu — rendered in nav-right, beside the social icons.
[[menu.info]]
name = "Info"
identifier = "info"
url = "#"
weight = 1
  [[menu.info]]
  parent = "info"
  name = "Colour"
  url = "/colour/"
  weight = 1
  # Placeholder sections — "coming soon" holding pages (layout: holding) until the real
  # Sources / Designers / Suppliers content is built. See content/{sources,designers,suppliers}/.
  [[menu.info]]
  parent = "info"
  name = "Sources"
  url = "/sources/"
  weight = 2
  [[menu.info]]
  parent = "info"
  name = "Designers"
  url = "/designers/"
  weight = 3
  [[menu.info]]
  parent = "info"
  name = "Suppliers"
  url = "/suppliers/"
  weight = 4
  [[menu.info]]
  parent = "info"
  name = "About"
  url = "/about/"
  weight = 5
  [[menu.info]]
  parent = "info"
  name = "Glossary"
  url = "/glossary/"
  weight = 6

[params.social]
  github = "https://github.com/Drummonds"
  rss = "index.xml"

[taxonomies]
category = "categories"
tag = "tags"
tartan = "tartans"
stripe = "stripes"

# doc: https://gohugo.io/getting-started/configuration-markup#highlight
# syntax style: https://xyproto.github.io/splash/docs/longer/all.html
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
    [markup.goldmark.extensions.typographer]
      disable = true
  [markup.highlight]
    guessSyntax = true
    hl_Lines = ""
    # lineNoStart = 1
    # lineNos = true
    # lineNumbersInTable = true
    noClasses = true
    style = "monokai" # github, dracula, vim, monokai
    tabWidth = 4

    